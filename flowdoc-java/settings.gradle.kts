rootProject.name = "flowdoc"

// v0.1 — spec + static core. Runtime agent / build plugins / spring-boot-starter
// arrive in later milestones (see docs/flowdoc-기획안.md §14 로드맵).
include("flowdoc-annotations")
include("flowdoc-core")
include("flowdoc-scanner")
